<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>ほうろうき</title>
  </head>
  <body>
   
    <br /><h1>立ち食い</h1>
    <section class="section">
      <div class="container">
        <figure class="hero-image">
          <img
            src="/images/tatigui1g.jpg"
            alt=""
            width="1600"
            height="900"
            loading="lazy"
            decoding="async"
          />
        </figure>
      </div>
    </section>

    <section class="narration">
      <img
        src="/images/tatigui1t.jpg"
        style="
        width: 90%;
        max-width: 600px;
        height: auto;
        display: block;
        margin: 0 auto 20px auto;
        border-radius: 15px;
        box-shadow: 0 0 10px rgba(0,0,0,0.3);
    "
      />

      <audio
        controls
        style="width: 90%; max-width: 600px; display: block; margin: 0 auto;"
      >
        <source src="/images/tatiguia.mp3" type="audio/mp3" />
        お使いのブラウザは audio タグに対応していません。
      </audio>
    </section>

    <style>
     
      h1 {
        text-align: center;
      }

      :root {
        --radius: 10px;
      }

      .section {
        padding-block: clamp(24px, 4vw, 56px);
      }

      .container {
        max-width: 960px;
        margin-inline: auto;
        padding-inline: clamp(16px, 4vw, 24px);
      }

      .hero-image {
        margin: 0;
      }

      .hero-image img {
        display: block;
        width: 100%;
        height: auto;
        border-radius: var(--radius);
        object-fit: cover;
      }

      .narration {
        margin-top: clamp(16px, 3vw, 28px);
        line-height: 1.75;
      }
    </style>
  </body>
</html>
